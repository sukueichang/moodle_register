# Moodle 登入頁改版執行手冊

本手冊提供給僅能透過瀏覽器操作 Moodle 後台的網站管理者使用（適用 Moodle 3.10.4 用詞）。

## 範圍

- 移除登入頁的 SAML2 登入區塊。
- 移除右上描述文字（例如瀏覽器/Cookies 提示、TM 外部登入說明）。
- 將本地端登入按鈕文字由 `TM employee Login` 改為可由管理員維護。
- 完成上架驗證與回退準備。

## 前置條件

- 你具備 Moodle「網站管理員」權限。
- 你可進入以下後台路徑：
  - `網站管理 > 外掛 > 認證 > 管理認證`
  - `網站管理 > 外觀 > 額外 HTML`
  - `網站管理 > 語言 > 語言自訂`
  - `網站管理 > 開發 > 清除所有快取`
- 安排低流量維護時段（約 10-20 分鐘）。

## 階段 0：變更前備份

1. 用無痕視窗開啟目前登入頁並截圖。
2. 記錄目前設定頁內容：
   - 認證外掛啟用狀態清單
   - 額外 HTML 內容
   - 現有語系自訂字串（若有）
3. 將截圖與備註附在變更單。

## 階段 1：停用登入頁 SAML2

1. 前往 `網站管理 > 外掛 > 認證 > 管理認證`。
2. 找到 `SAML2` 並停用。
3. 若畫面提示，請儲存變更。

預期結果：

- 登入頁不再顯示 SAML2 身分提供者按鈕。
- 若右側區塊來自 auth_saml2，該區塊會一併消失。

## 階段 2：移除描述文字區塊

1. 前往 `網站管理 > 外觀 > 額外 HTML`。
2. 檢查以下區塊，刪除登入頁相關自訂文字/HTML：
   - 在 BODY 標籤結尾之前
   - 在 BODY 標籤開始之後
   - HEAD 區段（若是以 script 注入登入頁文字）
3. 回到 `管理認證`，檢查是否有登入說明或自訂描述欄位，若有請清空。

預期結果：

- 類似 `[您的瀏覽器]`、`[Login from TM ...]` 的描述文字不再出現。

## 階段 3：讓按鈕文字可由管理員維護

使用「語言自訂」達到免部署改字。

1. 前往 `網站管理 > 語言 > 語言自訂`。
2. 選擇語系包 `zh_tw`。
3. 點選「開啟語言包進行編輯」。
4. 用以下關鍵字搜尋字串：
   - `TM employee Login`
   - `employee`
   - `login`
5. 找到後改成目標文字（例如：`員工登入`）。
6. 儲存語系包變更。

備註：

- 若搜尋不到，按鈕文字可能是主題模板硬編碼，需主機端改程式。
- 若搜尋得到，後續管理員可用同一路徑持續維護文字。

## 階段 4：清除快取

1. 前往 `網站管理 > 開發 > 清除所有快取`。
2. 執行一次完整清除。

## 階段 5：上架驗證清單

以全新無痕視窗驗證：

- [ ] SAML2 區塊已移除。
- [ ] 不再出現重複 `Customer Login`。
- [ ] `[您的瀏覽器]` 與 `[Login from TM ...]` 文字已移除。
- [ ] LDAP/本地端登入表單功能正常。
- [ ] 按鈕文字已由 `TM employee Login` 變更為目標文字。
- [ ] 各測一次錯誤密碼與正確登入（確認無認證回歸問題）。

跨瀏覽器冒煙測試：

- [ ] Chrome
- [ ] Edge

存證資料：

- 變更前登入頁截圖
- 變更後登入頁截圖
- 後台設定截圖（認證狀態、語言自訂）

## 回退計畫

若發生異常，依序回退：

1. 在 `管理認證` 重新啟用 SAML2。
2. 視需求還原 `額外 HTML` 被移除的文字。
3. 在 `語言自訂` 還原按鈕字串為 `TM employee Login`。
4. 再次執行「清除所有快取」。
5. 驗證登入頁恢復正常。

## 決策關卡：登入框是否置中於 Logo 下方

若完成上述設定後，登入框仍未置中：

- 多半是主題模板/CSS 控制（非純後台設定可完成）。
- 請使用 `THEME_CHANGE_REQUEST_TEMPLATE.md` 提交主機端最小改版需求。
